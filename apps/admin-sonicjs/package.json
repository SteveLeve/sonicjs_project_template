{
  "name": "example-admin",
  "private": true,
  "version": "1.0.0",
  "description": "Admin CMS for Test Community Site",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply example_db",
    "migrate:local": "wrangler d1 migrations apply example_db --local"
  },
  "dependencies": {
    "sonicjs": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.76.0"
  }
}